<!DOCTYPE html>
<html lang="{{ lang | default('en') }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Kodudo{% endblock %}</title>
    <style>
        body { font-family: sans-serif; line-height: 1.6; max-width: 800px; margin: 0 auto; padding: 2rem; }
        header { border-bottom: 1px solid #ccc; margin-bottom: 2rem; }
        footer { margin-top: 2rem; border-top: 1px solid #ccc; padding-top: 1rem; font-size: 0.8em; color: #666; }
        .metadata { background: #f4f4f4; padding: 1rem; border-radius: 4px; }
    </style>
    {% block head %}{% endblock %}
</head>
<body>
    <header>
        <h1>{% block header %}{{ title }}{% endblock %}</h1>
    </header>

    <main>
        {% block content %}{% endblock %}
    </main>

    <footer>
        <p>Generated by Kodudo{% if meta.version %} (Schema v{{ meta.version }}){% endif %}</p>
    </footer>
</body>
</html>
